# Strategy Summary — {{run_id}}

> Generated by `scripts/render_report.py` on {{generated_at}}.
> Every numeric value below comes from a stage artifact; do not hand-edit.

## 1. Sample scope

- Time window: {{time_window}}
- Total samples: {{total_samples}} (observable: {{observable_samples}}; rejected: {{rejected_samples}})
- Bad definition: `{{target}} == 1`
- Overall bad rate (observable): {{overall_bad_rate}}
- Split method: {{split_method}} (random_state={{random_state}})
- Excluded columns: {{exclude_cols}}

See `sample_profile.csv`, `sample_split_log.csv`.

## 2. Field audit conclusion

- Total candidate features: {{candidate_feature_count}}
- Dropped for leakage / availability: {{leakage_drop_count}}
- Dropped for instability supply: {{unstable_drop_count}}

See `field_audit.csv`, `leakage_audit.csv`.

## 3. Data quality

- Usable features after stage 1: {{stage1_kept}}
- Dropped this stage: {{stage1_dropped}} (constants, missing≥90%, etc.)

See `data_quality.csv`, `outlier_summary.csv`, `feature_drop_reason.csv`.

## 4. Variable validity

- Top features by IV: {{top_iv_features}}
- IV / KS / AUC / monotonicity / leakage findings:

See `feature_quality.csv`, `monotonicity_check.csv`.

## 5. Single-variable candidate rules

- Total candidates: {{single_candidate_count}}
- Passed `filter_rule_candidates`: {{single_passed_count}}
- Cross-period lift direction consistency: {{single_consistent_count}}
- Unstable / not recommended: {{single_unstable_count}} (see `decision_log.csv`)

See `single_rule_candidates.csv`, `single_var_rule_eval.csv`.

## 6. Multi-rule combinations

- Top combinations: {{top_combo_rules}}
- Joint hit / bad / lift improvement vs. single rules: {{combo_marginal_gain}}

See `rule_combination_candidates.csv`.

## 7. Decision-tree rules

- Rules generated: {{tree_rule_count}}
- Action distribution: reject={{tree_reject}}, review={{tree_review}}, observe={{tree_observe}}
- Confidence distribution: HIGH={{tree_high}}, MEDIUM={{tree_medium}}, LOW={{tree_low}}
- Overlap notes: see `rule_overlap_matrix.csv`

See `decision_tree_rules.csv`, `decision_tree.dot`, `decision_tree.png`.

## 8. Waterfall

| Step | Added rule | Incr. hit rate (train/test/OOT) | Incr. captured bad rate | Notes |
|---|---|---|---|---|
| {{waterfall_rows}} |

See `waterfall_simulation.csv`, `waterfall_comparison.csv`, `waterfall_simulation_full.csv`.

## 9. Strategy simulation

- Final strategy: `{{strategy_id}}` ({{rule_count}} rules)
- Observable: reject_rate={{obs_reject_rate}}, pass_bad_rate={{obs_pass_bad_rate}}, lift={{obs_lift}}
- Full-population (with reject inference): reject_rate={{full_reject_rate}}, pass_bad_rate={{full_pass_bad_rate}}
- Monthly stability: {{monthly_stability}}
- Segment stability: {{segment_stability}}

See `strategy_rules.csv`, `rule_simulation.csv`, `rule_simulation_full.csv`, `strategy_comparison.csv`, `monthly_rule_simulation.csv`, `segment_rule_simulation.csv`.

## 10. Go-live recommendation

- Recommendation: {{recommendation}}  (one of: go_live | grey_release | observe | reject)
- Supporting evidence IDs: {{supporting_evidence_ids}}

> The agent fills this paragraph. Every claim must cite a `rule_id` or `evidence_id`.

## 11. Monitoring plan

See `monitoring_plan.csv`. Frequencies and alert thresholds set per rule and per rule-variable.

## 12. Risk notes

> Sample bias, ambiguous variable meaning, cross-period drift, segment differences, compliance concerns. If segment columns were missing, note the cross-segment validation limitation here.

---

**Evidence integrity:** every row of `confidence_evidence.csv` has `train_value`, `test_value`, `oot_value` populated. Total evidence rows: {{evidence_row_count}}.

**Artifact integrity:** see `manifest.json` for SHA-256 of every artifact. Run is {{run_status}}.
